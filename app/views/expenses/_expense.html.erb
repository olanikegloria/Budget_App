<li id="<%= dom_id expense %>" class='expense-item'>
  <p>
    <strong>Name:</strong>
    <%= link_to expense.name, category_expense_path(@category, expense) %>
  </p>

  <p>
    <strong>Amount:</strong>
      $<%= expense.amount %>
  </p>

</li>
